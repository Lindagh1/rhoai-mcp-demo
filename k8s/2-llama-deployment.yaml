---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-stack
  labels:
    app: llama-stack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llama-stack
  template:
    metadata:
      labels:
        app: llama-stack
    spec:
      containers:
      - name: llama-stack
        image: llamastack/distribution-starter:latest
        ports:
        - containerPort: 8321  # <-- CHANGEMENT ICI (le vrai port)
        env:
        - name: LLAMA_STACK_CONFIG
          value: "/app/config/config.yaml"
        - name: RHOAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: rhoai-api-key
              key: api-key
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
      volumes:
      - name: config-volume
        configMap:
          name: llama-stack-config
---
apiVersion: v1
kind: Service
metadata:
  name: llama-stack-service
spec:
  selector:
    app: llama-stack
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 8321  # <-- ET CHANGEMENT ICI (la redirection)